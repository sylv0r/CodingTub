import React from 'react';
import './SearchPage.scss';


class MaComponent extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      numvideo: 5 // le nombre de vidéos à afficher
    };
    this.state = {
      numchaine : 5
    }; 
  }
}
function NDRVOD() {
                  if (valeur == video){
                      render(); {
                        const videos = [];
                        for (let i = 0; i < this.state.numvideo; i++) {
                          videos.push(<div key={i} className="video">Vidéo {i + 1}</div>);
                        }
                        return (
                          <div>
                            {videos}
                          </div>
                        );
                      }
                    }

                  if (value == chaine ){
                          render(); {
                            const chaine = [];
                            for (let i = 0; i < this.state.numchaine; i++) {
                              chaine.push(<div key={i} className="chaine">chaine {i + 1}</div>);
                            }
                            return (
                              <div>
                                {videos}
                              </div>
                            );
                          }
                        }
                      }
        
    
      
      export default MaComponent; 