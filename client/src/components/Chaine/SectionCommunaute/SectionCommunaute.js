import React from 'react'

export default function SectionCommunaute(){
    // state

    // comportements 

    // affichage (render)
    return <div>
        <p>Voici la section Communauté, elle te permet d'avoir les dernières actualitées sur la chaîne</p>
    </div>
}