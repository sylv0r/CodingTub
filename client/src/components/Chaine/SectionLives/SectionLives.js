import React from 'react'

export default function SectionLives(){
    // state

    // comportements 

    // affichage (render)
    return <div>
        <p>Mettre le composant des Lives</p>
    </div>
}