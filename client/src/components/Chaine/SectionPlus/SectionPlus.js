import React from 'react'

export default function SectionPlus(){
    // state

    // comportements 

    // affichage (render)
    return <div>
        <p>Voici la section À Propos, elle te permet d'avoir plus d'infos sur la chaîne</p>
    </div>
}