export default function SideBarButton({name, logo, link}){

    //state

    //comportement

    //render

    return(
        <div id="SideBarButton">
            <a href={link}>{name}</a>
            

        </div>
    )


}